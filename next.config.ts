// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  // ❌ REMOVE THIS LINE IF IT EXISTS:
  // output: 'export',  // DELETE THIS!
  
  // ✅ Keep these if you have them:
  images: {
    unoptimized: true, // Keep if you use images
  },
}

module.exports = nextConfig